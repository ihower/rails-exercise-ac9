<% if like %>
  <%= link_to "-1", topic_like_path(@topic, like), :method => :delete, :class => "btn btn-danger", :remote => true %>
<% else %>
  <%= link_to "+1", topic_likes_path(@topic), :method => :post, :class => "btn btn-primary", :remote => true %>
<% end %>
